import {UIUtils} from "../UIUtils";

/**
 * Panel with information about selected tile (hexagon field)
 * @param owner
 * @constructor
 */
export const TileInfoPanelControllerConstructor = (owner) => {
    return {}
}