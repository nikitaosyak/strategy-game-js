import {UIUtils} from "../UI";

/**
 * Panel with information about selected tile (hexagon field)
 * @param {Element} parent
 * @constructor
 */
export const TileInfoPanelControllerConstructor = (parent) => {
    return {}
}